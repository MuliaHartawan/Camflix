const register = require('./register');
const login = require('./login');
const update = require('./update');
const user = require('./user');
const logout = require('./logout');

module.exports = {
    register, login, update, user, logout
}